
  # E-wallet Login Page



  ## Running the code

  Run `npm i` to install the dependencies.


  run the server
  1st open terminal in vs code
  2nd cd backend
  3rd Run `npm run dev` to start the  server.

  start the web
  1st open new terminal
  2nd npm run dev to start web
  
